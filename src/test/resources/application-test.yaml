spring:
  datasource:
    url: jdbc:postgresql://localhost:15433/shiba-test
    username: shiba
    hikari:
      maximum-pool-size: 2

client:
  keystore: src/test/resources/test-keystore.jks

sender-email: sender@email.org
security-email: security@email.org
audit-email: audit@email.org

mail-gun:
  url: mailgun-url

submit-via-email: true