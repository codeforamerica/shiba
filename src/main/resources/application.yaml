spring:
  datasource:
    url: jdbc:h2:mem:testdb;MODE=PostgreSQL;
    driverClassName: org.h2.Driver
    username: sa
    password: password
  profiles:
    include: secrets

client:
  keystore: src/main/resources/local-keystore.jks